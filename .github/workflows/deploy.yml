name: Deploy to GitHub Pages (Vite)

on:
  push:
    branches:
      - master   # 你的分支是 master 就寫 master

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

      # Vue Router 刷新 404：自動把 index.html 複製為 404.html
      - name: SPA fallback (copy index.html to 404.html)
        run: cp dist/index.html dist/404.html

      # 自訂網域 CNAME（保險；也可以改用 Settings > Pages 設定）
      - name: Add CNAME
        run: echo "yoake1.tw" > dist/CNAME

      # 上傳 Pages artifact
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
